{
  "name": "bhangmeter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "bundle install && jekyll build",
    "deploy": "wrangler publish"
  },
  "dependencies": {
    "@cloudflare/wrangler": "^1.19.12"
  }
}